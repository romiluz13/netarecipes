rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // מאפשר גישה מלאה לכל המשתמשים
      allow read, write: if true;
    }
  }
} 